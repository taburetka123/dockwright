{
  "id": "c09_rename_equiv_py",
  "language": "python",
  "label": "clean",
  "defect_class": "clean",
  "intent": "average(values) returns the arithmetic mean of a non-empty list.",
  "diff": [
    "--- a/stats.py",
    "+++ b/stats.py",
    "@@ -1,3 +1,3 @@",
    " def average(values):",
    "-    s = sum(values)",
    "-    return s / len(values)",
    "+    total = sum(values)",
    "+    return total / len(values)"
  ],
  "why": "Local variable rename; identical logic."
}
